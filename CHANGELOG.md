# 变更记录

## 0.2.3

### 用户感知功能

- 联网搜索请求自动注入服务器当前具体时间、时区名称和 UTC 偏移，减少模型误判当前年份的问题。
- DeepSeek 会以注入时间处理“今天”“最新”“近期”“今年”等相对时间，并核对搜索结果发布日期。

### 开发侧修复

- 在统一请求管道中识别 Web Search server tool，覆盖联网搜索、网页读取、通用代理和搜索测试命令。
- 无搜索工具的纯推理请求和连接测试保持原有行为。

## 0.2.2

### 用户感知功能

- 新增“最大输出长度”配置，默认 `4096`，可用于深度思考和长总结。
- WebUI 主要配置新增通俗中文提示，并隐藏无需手动修改的配置版本。
- API、搜索工具和输出截断错误改为清晰中文提示，完整异常不再回显到聊天。
- 网页读取只接受有效 HTTP/HTTPS URL，避免无效请求产生费用。

### 开发侧修复

- 使用 `model_dump()` 兼容 Anthropic SDK 内容块对象，正确提取并按 URL 去重搜索来源。
- 识别 `max_uses_exceeded`、`unavailable` 等 Web Search server tool 错误。
- 高于当前插件版本的配置原样保留，不执行降级迁移或删除未知字段。
- 将 `maibot-plugin-sdk` 依赖限制为 `>=2.0.0,<3.0.0`。
- 新增默认跳过的真实 API ping 与单次搜索集成测试。
