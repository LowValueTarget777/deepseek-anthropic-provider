# 变更记录

## 0.2.5

- 严格核验 fetch 目标页，保留查询字符串、路径参数及片段，不再接受同站首页或父子页面。
- 搜索失败、空回复、非正常结束和截断统一使用类型化错误；测试命令和集成测试不再把错误文字当作成功。
- 截断的可靠部分内容带明确提示返回，缺少来源的模型文本不返回。
- 增加包含排队的请求总时限，关闭 SDK 自动重试；复用连接池并正确处理密钥变更和卸载清理。
- 增加并发限制、有界等待队列及同会话在途请求合并，不缓存已完成答案。
- 新增快速、标准、深入搜索档位及结构化范围偏好，受管理员次数和思考预算限制。
- 补充来源质量、时间核验、渐进检索提示；动态时间移至用户输入末尾，保留固定提示前缀。
- 默认模型更新为 `deepseek-flash`，迁移旧 Flash 配置并更新 Pro 路由说明；新增 `low` 思考选项。
- 增加请求标识、来源数量与耗时日志；API 异常只记录类型和状态，不记录原始响应。
- SDK 最低版本同步为已验证结构化 Tool 参数的 `2.5.2`；保留六个配置标签页及无密钥示例。

## 0.2.4

### 用户感知功能

- WebUI 配置页改为顶部标签页，保留基础设置、密钥设置、模型设置、思考设置、联网搜索和调试日志分组。
- `fetch_page` 和 `search_and_summarize` 只接受包含有效结果的 server web search；搜索失败或空结果不再返回模型补写文本。
- `fetch_page` 会核验搜索来源与目标 URL 相关，避免把无关页面内容当作指定网页摘要。
- `fetch_page` 提示词更明确：无法读取网页时直接说明失败，不凭常识或训练数据补写内容。

### 开发侧修复

- 仓库改为提交 `config.example.toml`，不再跟踪本地运行用的 `config.toml`。
- 新增 `invalid_input` 搜索错误的中文提示。
- DeepSeek 请求增加 120 秒显式超时，避免网络异常时长期占用 Tool 调用。
- 版本同步到 `0.2.4`。

## 0.2.3

### 用户感知功能

- 联网搜索请求自动注入服务器当前具体时间、时区名称和 UTC 偏移，减少模型误判当前年份的问题。
- DeepSeek 会以注入时间处理“今天”“最新”“近期”“今年”等相对时间，并核对搜索结果发布日期。

### 开发侧修复

- 在统一请求管道中识别 Web Search server tool，覆盖联网搜索、网页读取、通用代理和搜索测试命令。
- 无搜索工具的纯推理请求和连接测试保持原有行为。

## 0.2.2

### 用户感知功能

- 新增“最大输出长度”配置，默认 `4096`，可用于深度思考和长总结。
- WebUI 主要配置新增通俗中文提示，并隐藏无需手动修改的配置版本。
- API、搜索工具和输出截断错误改为清晰中文提示，完整异常不再回显到聊天。
- 网页读取只接受有效 HTTP/HTTPS URL，避免无效请求产生费用。

### 开发侧修复

- 使用 `model_dump()` 兼容 Anthropic SDK 内容块对象，正确提取并按 URL 去重搜索来源。
- 识别 `max_uses_exceeded`、`unavailable` 等 Web Search server tool 错误。
- 高于当前插件版本的配置原样保留，不执行降级迁移或删除未知字段。
- 将 `maibot-plugin-sdk` 依赖限制为 `>=2.0.0,<3.0.0`。
- 新增默认跳过的真实 API ping 与单次搜索集成测试。
